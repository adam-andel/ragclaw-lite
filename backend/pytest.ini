[pytest]
asyncio_mode = auto
testpaths = tests
# Manual live-server smoke scripts live under tests/integration/manual and are
# NOT part of the pytest suite (they require a running backend + LLM). Keep them
# out of collection so they don't surface as fixture errors.
addopts = --ignore=tests/integration/manual
