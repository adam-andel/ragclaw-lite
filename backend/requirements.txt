# Floating (unpinned) backend dependencies for the DEV image.
# The dev build installs from this file, then `pip freeze` regenerates
# requirements.lock (pinned) for production to consume — mirroring
# frontend's package.json -> pnpm-lock.yaml flow.
# NOTE: Do NOT list the project itself here; it runs via PYTHONPATH=/app/backend.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
markdown>=3.6
jieba>=0.42.1
rank-bm25>=0.2.2
tiktoken>=0.7.0
httpx>=0.27.0
sse-starlette>=2.0.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
cryptography>=42.0.0
langgraph>=0.2.0
langchain-core>=0.3.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
openpyxl>=3.1.0
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
lxml>=5.0.0
striprtf>=0.0.26
ebooklib>=0.18
nbformat>=5.10.0
extract-msg>=0.50.0
croniter>=2.0.0
pytz>=2024.1
