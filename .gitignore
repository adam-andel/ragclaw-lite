# Python
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/
.venv/
venv/
*.log

# Node
node_modules/
frontend/dist/
pnpm-workspace.yaml
# Lockfile intentionally untracked: keeps the one-command deploy barrier low. Deps are
# resolved from package.json at build time (pnpm --no-frozen-lockfile); direct deps are
# pinned to exact versions. The dev self-heal still writes a local lockfile (unused by git).
frontend/pnpm-lock.yaml
*.tsbuildinfo

# Data
data/*
!data/uploads/
data/uploads/*
!data/uploads/.gitkeep

# Environment
.env

# Docker secrets (generated by bin/sh/lib/gen-secrets.sh, 600 perms, gitignored)
secrets/

# Runtime state (watcher PID file / log)
.run/

# IDE
.vscode/
.idea/
.trae/
.codebuddy/
.workbuddy
.claude/
CLAUDE.md
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
