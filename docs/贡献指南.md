# 贡献指南

> 感谢你愿意参与 **RAGClaw**。本文档说明如何规范地报告问题、提交改动与参与评审。英文版见 [CONTRIBUTING.md](CONTRIBUTING.md)。

---

## 1. 行为准则

大家以尊重、合作的方式交流。涉及人身攻击、骚扰、歧视等不友好行为，维护者有权拒绝合并或要求整改。

---

## 2. 简短的原则

- 改动尽量小而聚焦，一次 PR 只解决一个问题；
- 先讨论、后实现。较大的改动（新增功能、架构调整、破坏性变更）**先开 issue**，达成一致后再动手；
- 只向 `main` 分支发起 PR。

---

## 3. 提交信息规范

提交信息一律用**英文**，采用 `type(scope): subject` 结构。`scope` 为可选项，在可辨识时填写（如 `chat`、`egress`、`schema`、`docs`）。

| type | 用途 |
|------|--------|
| `feat` | 新功能 |
| `fix` | 修复 |
| `refactor` | 重构，行为不变 |
| `docs` | 文档 |
| `test` | 新增或修正测试 |
| `chore` | 杂项，如依赖、构建 |
| `perf` | 性能优化 |

示例：

```
fix(chat): handle zero-quota (unlimited) correctly on continue
refactor(egress): extract egress broker into independent ./egress directory
docs: sync secrets contract docs with current implementation
```

**不要**提交与本 PR 无关的文件；不要把 `.env`、密钥等敏感文件纳入版本控制。

---

## 4. 代码规范

- 代码注释一律使用**英文**；
- `backend/`（Python）：遵循现有的代码风格，新依赖需同步更新 `requirements.txt` / `requirements-dev.txt`；
- `frontend/`（Vue）：遵循现有的 `package.json` 脚本与风格；
- **脚本一致性**：`bin/psl`（Windows）与 `bin/sh`（macOS/Linux）需保持配置、行为对齐。改动任一脚本时，应同步审查另一套。psl 属有意精简，不包含开发模式。

---

## 5. 测试要求

改动必须通过现有测试；涉及逻辑变更时，应在 `backend/tests/` 下**补测或更新**对应用例（`unit/`、`api/`、`security/`、`integration/`）。

本项目测试**只在容器模式**下运行（本地 Python 执行不受支持）。运行方式：

```
# macOS / Linux
bash bin/sh/run_all_tests.sh

# Windows（CMD / PowerShell）
bin\psl\run_all_tests.ps1
```

尤其注意 `security/` 下的用例（鉴权、RBAC、会话隔离、注入、IDOR 等）。若改动涉及这些逻辑，必须补测并在 PR 中说明。

---

## 6. 安全敏感区域

下列改动会直接触及安全边界，**必须在 PR 描述中说明安全影响**：

- 执行沙盒（`mcp-repl` 容器、`read_only` + `seccomp` + `cap_drop`）；
- 出站网络策略（`egress/`，注入代理、allowlist）；
- API 密钥管理与注入（SKILL 注入代理、密钥环境变量）；
- SKILL 上传 / 适配器（`.ragclaw/`、`shim.py`）；
- 用户隔离（UID）、RBAC / 权限、会话隔离。

---

## 7. Pull Request 流程

1. Fork 仓库，在 `main` 之外开一个命名清晰的分支；
2. 提交符合第 3 节规范的信息；保持分支可合并、提交尽量语义化；
3. 提交 PR 到 `main`，在描述中说明：改了什么、为什么、如何验证、是否触及第 6 节安全区域；
4. 通过 CI / 测试后，维护者会评审；根据反馈修改并更新，不搞破坏性 rebase 之外的混乱历史。

---

## 8. 许可证

本项目采用 **Apache License 2.0**（见 `LICENSE`）。提交即视为你同意在相同的开源协议下贡献；大段引入第三方代码时请注明来源与许可证。