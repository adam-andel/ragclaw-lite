export default {
  title: 'System Settings',
  subtitle: 'LLM · Server · Retrieval Debug · Plugins · Super Admin only',
  saveConfig: 'Save Configuration',
  nav: {
    llm: 'LLM',
    embeddingModel: 'Embedding Model',
    systemPrompt: 'System Prompt',
    sandboxNetwork: 'Sandbox Network',
    replAuth: 'REPL Auth',
    jwtAuth: 'JWT Auth',
    https: 'HTTPS',
    plugins: 'Plugins',
  },
  alertTitle: 'LLM API Key Not Configured',
  alertDesc: 'Please enter the LLM provider API Key first; otherwise the system cannot start a conversation. Takes effect immediately after entry, no restart needed.',
  embeddingModel: 'Embedding Model',
  embeddingModelTip: 'Uses the local Embedding model by default.',
  embeddingModelMgmt: {
    title: 'Embedding Model Management',
    desc: 'The local Embedding model is used for document vectorization and semantic retrieval. It is NOT pre-installed in the image; pick a model and click "Download & Install" before first use (~130MB–1.3GB, requires network). Downloaded files live in the persistent volume and survive container restarts. Different models have different vector dimensions; switching to an incompatible dimension clears all existing vector indexes.',
    select: 'Select Model',
    selectPlaceholder: 'Choose an Embedding model to use',
    apply: 'Save & Switch',
    currentLabel: 'Current',
    installedList: 'Installed Models',
    status: 'Install Status',
    statusInstalled: 'Installed',
    statusNotInstalled: 'Not Installed',
    statusDownloading: 'Downloading',
    statusPaused: 'Paused',
    statusFailed: 'Download Failed',
    statusCancelled: 'Cancelled',
    download: 'Download & Install',
    downloading: 'Download started, please wait…',
    alreadyDownloading: 'Already downloading, please wait',
    pause: 'Pause',
    resume: 'Resume',
    cancel: 'Cancel',
    paused: 'Download paused',
    resumed: 'Download resumed',
    cancelled: 'Download cancelled',
    cancelledTip: 'Download cancelled. The partially downloaded files are kept locally — click "Download & Install" again to continue.',
    delete: 'Delete Model',
    deleted: 'Local model deleted',
    installedTip: 'Model is ready and can be used for vectorization and retrieval.',
    notInstalledTip: 'Model not installed yet; please install it before uploading docs or searching.',
    switched: 'Model switched',
    switchedNotInstalled: 'Model switched, but the new model is not downloaded yet. Click "Download & Install" below.',
    switchedCleared: 'Model switched and all vector indexes cleared. Please re-upload / rebuild your knowledge bases.',
    conflictTitle: 'Incompatible Vector Dimension',
    conflictConfirm: 'Switching will change the vector dimension ({existing} → {neu}). The {count} existing vectors will be cleared and knowledge bases must be re-uploaded / rebuilt. Continue?',
    conflictOk: 'Clear & Switch',
    switchBtn: 'Switch',
    switchReindexBtn: 'Switch & Re-index',
    rebuildIndexBtn: 'Re-index Now',
    selectFirst: 'Please select a model first',
    installFirstHint: 'Please download & install this model before switching',
    dimensionConflictTip: 'The selected model "{model}" has a different dimension ({existing} → {neu}) from the current one. Switching will clear all {count} existing vector indexes and rebuild them.',
    reindexTitle: 'Re-index Progress',
    reindexStarted: 'Background re-index task started',
    reindexRunning: 'Re-index is already running',
    deletingVectors: 'Deleting old vectors…',
    reindexDone: 'Re-index complete',
    reindexFailed: 'Re-index failed',
    reindexPendingDownload: 'Re-index will start automatically once the model download finishes',
    reindexPhases: {
      deleting: 'Deleting old vectors…',
      reembedding: 'Re-embedding {total} document(s)…',
      nothing: 'No completed documents - nothing to re-embed',
      processed: 'Processed {i}/{total}: {filename}',
      completed: 'Re-embedding complete - {total} document(s) processed',
      err_model_not_installed: 'Embedding model not installed - cannot re-embed',
      err_delete_failed: 'Failed to delete old vectors',
      err_db_open: 'Cannot open database',
      err_reembed_failed: 'Re-embedding failed',
    },
    downloadProgress: 'Download progress',
    rebuildConfirmTitle: 'Confirm Re-index',
    rebuildConfirmContent: 'This will re-embed ALL documents (including chunked, not-yet-vectorized ones) with the current model "{model}" and rebuild every vector index. This may take a while. Continue?',
    rebuildConfirmOk: 'Start Re-index',
    deleteConfirmTitle: 'Confirm Delete Model',
    deleteConfirmContent: 'This will delete all local files and the install record for model "{model}". You will need to download it again before use. Continue?',
    deleteConfirmOk: 'Delete',
  },
  llmTitle: 'LLM Configuration',
  llmDesc: 'Configure the LLM provider, API key, and inference parameters. Changes take effect immediately, no restart needed.',
  providerLabel: 'LLM Provider',
  providerQwen: 'Qwen (Alibaba)',
  providerOllama: 'Ollama (Local)',
  providerCustom: 'Custom',
  apiKey: {
    currentEnv: 'Current: {key} (from .env file, leave blank to keep)',
    current: 'Current: {key} (leave blank to keep)',
    placeholder: 'Enter API Key (first time)',
  },
  modelName: 'Model Name',
  tip: {
    temperature: 'Controls the randomness and creativity of LLM output. Range 0~1.<br/><b>0</b> = most deterministic, consistent answers; <b>1</b> = most random.<br/>For RAG, <b>0.1~0.5</b> is recommended so the LLM strictly follows retrieved docs and reduces free-form generation.<br/><b>Note</b>: this only affects the "final answer" wording; tool-calling (Function Calling) is hardcoded to a low temperature in code and is not affected by this setting.',
    maxTokens: 'Maximum tokens for a single LLM output (≈ Chinese chars × 1.5~2).<br/>Too small truncates the answer; too large wastes quota.<br/>For RAG, <b>1024~4096</b> is usually enough and also limits the final answer length.',
    maxConcurrency: 'Upper limit of concurrent sessions sending requests to the LLM API; excess enters a queue.<br/>Set per provider tier; OpenAI Tier 1 is usually 3~5.<br/><b>Takes effect immediately</b> without interrupting in-flight requests.',
    cacheTtl: 'Cache validity (seconds) for identical questions.<br/>Default 3600s (60 min). Set 0 to fully disable caching.<br/><b>Takes effect immediately</b> and does not affect already-cached entries (expiry judged by each entry\'s creation time).',
    contextWindow: 'Max context window (tokens) of the current model. Used as the denominator for the "context usage" bar on the chat page.<br/>Varies widely by model (e.g. gpt-4o = 128000, Claude = 200000, some local/Chinese models = 32000/64000).<br/><b>Takes effect immediately</b>.',
    agentRoundQuota: 'For a single Agent run (all chats and cron jobs), the maximum number of "think → call tool → observe" loops.<br/>Execution stops and outputs current progress once the limit is reached.<br/>Increase it when tasks are complex or the LLM often emits broken code (e.g. Windows path escaping, null bytes) to reduce failures from exhausted rounds.<br/>Set to 0 for unlimited rounds (recommended only for trusted long-running tasks).<br/>Clicking "Continue" adds the same number of rounds on top of the current quota.<br/><b>Takes effect immediately</b>.',
  },
  maxConcurrency: 'Max Concurrency',
  cacheTtl: 'Cache TTL',
  contextWindow: 'Context Window',
  agentRoundQuota: 'Tool-call Round Quota',
  agentRoundQuotaHint: 'Each chat/cron run allows up to {n} tool-decision rounds',
  agentRoundQuotaUnlimited: 'Unlimited rounds (0)',
  cacheSecondsApprox: '{seconds} s ≈ {minutes} min',
  testConnection: 'Test Connection',
  test: {
    success: '✅ Connected — Model: {model}, Reply: "{reply}"',
    failed: '❌ Connection failed: {error}',
    error: '❌ Request error: {message}',
  },
  systemPrompt: 'LLM System Prompt',
  systemPromptTip: 'System prompt used for RAG and default Agent replies.<br/>Takes effect immediately, no restart needed.',
  systemPromptTitle: 'System Prompt',
  systemPromptDesc: 'Set the system prompt injected into every conversation, plus the prompt language used by the agent graph. Changes take effect immediately, no restart needed.',
  pluginsTitle: 'Plugin Management',
  pluginsDesc: 'These are the file formats supported for upload by the document management module.',
  agentPromptLang: 'Agent Prompt Language',
  agentPromptLangTip: 'Controls the language of Agent Graph internal prompts (intent routing + tool forced JSON).<br/>Default <b>Chinese</b>; switch to <b>English</b> to compare instruction-following on English-dominant models (GPT / Claude / DeepSeek, etc.).<br/>Takes effect immediately, no restart, good for A/B comparison.',
  promptLang: {
    system: 'Follow System',
    en: 'English',
    zh: 'Chinese',
  },
  sandboxTitle: 'Sandbox / REPL Execution Environment (Network Policy & File Retention)',
  sandboxDesc: 'Controls whether LLM-generated code can access external networks in the sandbox. Default <b>deny</b> (fully blocked, safest). After selecting <b>allowlist</b>, you can list allowed domains (comma- or newline-separated); Python code may only reach allowlisted domains, and direct IP connections are blocked (DNS rebinding protection). Changes are hot-reloaded via the MCP service, <b>taking effect immediately, no restart</b>.',
  networkMode: 'Network Policy Mode',
  networkDeny: 'deny (default, blocks all network access)',
  networkAllow: 'allow (open, debug only)',
  networkAllowlist: 'allowlist (allowlisted domains only)',
  allowDomains: 'Allowed Domains',
  saveSandbox: 'Save Sandbox Network Policy',
  currentMode: 'Current: {mode}',
  noDomainsConfigured: 'No domains configured',
  replAuthTitle: 'REPL Execution Environment Identity Secret (REPL_AUTH_SECRET)',
  replAuthDesc: 'LLM-generated code runs in the sandbox under a <b>dedicated Linux account</b> per user, isolating users from one another. The Backend signs this secret and the sandbox verifies it. <b>A strong secret is auto-generated on first boot</b> — no manual .env setup needed. Click "Generate" to rotate it (takes effect immediately).',
  replAuthSecretLabel: 'REPL_AUTH_SECRET',
  replAuthPlaceholder: 'Click "Generate" to create a new secret',
  replAuthGenerate: 'Generate New Secret',
  replAuthSave: 'Save',
  replAuthCopy: 'Copy',
  replAuthCopied: 'Copied to clipboard',
  replAuthCopyFailed: 'Copy failed, please select and copy manually',
  replAuthEmpty: 'Secret cannot be empty',
  replAuthTooShort: 'Secret must be at least 16 characters',
  replAuthOn: 'Enabled',
  replAuthOff: 'Disabled (single-account mode)',
  replAuthStatus: 'Current status: {on}',
  replAuthStatusOn: 'Secret is active and hot-reloaded into the sandbox; per-user isolation is on',
  replAuthStatusRestart: 'Saved, but sandbox hot-reload failed; takes effect after restarting the mcp-repl container',
  replAuthUnsaved: 'Unsaved changes',
  jwtAuthTitle: 'JWT Session Signing Secret (JWT_SECRET)',
  jwtAuthDesc: 'All login-session JWT tokens are signed with this secret. <b>A strong secret is auto-generated on first boot</b> — no manual .env setup needed. Click "Generate" to rotate it (takes effect immediately). <b>Note: after rotation every issued session (including yours) becomes invalid and must re-login.</b>',
  jwtAuthSecretLabel: 'JWT_SECRET',
  jwtAuthPlaceholder: 'Click "Generate" to create a new secret',
  jwtAuthGenerate: 'Generate New Secret',
  jwtAuthCopy: 'Copy',
  jwtAuthCopied: 'Copied to clipboard',
  jwtAuthCopyFailed: 'Copy failed, please select and copy manually',
  httpsTitle: 'HTTPS Secure Access',
  httpsDesc: 'When enabled, the nginx reverse proxy terminates TLS on <b>443</b> and the entire site (API + frontend) is served over HTTPS, with HSTS set. The certificate and key are encrypted at rest by the backend and written to a shared volume for nginx to read.',
  httpsEnableLabel: 'Enable HTTPS',
  httpsCertLabel: 'Certificate (fullchain PEM)',
  httpsCertPlaceholder: 'Paste the full certificate chain (including intermediates) as PEM',
  httpsKeyLabel: 'Private Key (PEM)',
  httpsKeyPlaceholder: 'Paste the private key as PEM (unencrypted)',
  httpsSave: 'Save',
  httpsStatusOn: 'HTTPS enabled, entire site served over HTTPS',
  httpsStatusOff: 'HTTPS disabled (plain HTTP)',
  httpsCertInfo: 'Subject: {subject} · Expires: {expires}',
  httpsAccessHint: 'After enabling, access via https://<your-host>:<RAGCLAW_HTTPS_PORT or `docker compose port nginx 443`>',
  httpsUnsaved: 'Unsaved changes',
  httpsNeedCert: 'Certificate and private key are required to enable HTTPS',
  httpsSaved: 'HTTPS configuration saved',
  msg: {
    loadConfigFailed: 'Failed to load config',
    loadSandboxFailed: 'Failed to load sandbox network config',
    configSaved: 'Configuration saved, takes effect immediately',
    saveFailed: 'Failed to save',
    sandboxSavedHot: 'Sandbox network policy saved and hot-reloaded, takes effect immediately',
    sandboxSavedRestart: 'Saved (MCP unreachable; re-save after confirming mcp-repl is up, or it auto-retries every 60s)',
    loadReplAuthFailed: 'Failed to load REPL auth secret',
    loadHttpsFailed: 'Failed to load HTTPS config',
    loadJwtAuthFailed: 'Failed to load JWT secret',
    replAuthSavedHot: 'REPL_AUTH_SECRET saved and hot-reloaded; per-user isolation takes effect immediately',
    replAuthGeneratedHot: 'New REPL_AUTH_SECRET generated and hot-reloaded, takes effect immediately',
    replAuthSavedRestart: 'Saved (sandbox hot-reload failed, takes effect after restarting the mcp-repl container)',
    jwtAuthGeneratedHot: 'New JWT signing secret generated and active immediately; all existing sessions must re-login',
    saving: 'Saving…',
    saved: 'Saved',
    saveFailedPrefix: 'Save failed: ',
    saveFailedNetwork: 'Network error, please check your connection',
    saveFailedTimeout: 'Request timed out, please retry',
    saveFailedServer: 'Internal server error, please retry',
    saveFailedBadRequest: 'Invalid request parameters',
    saveFailedUnauthorized: 'Session expired, please log in again',
    saveFailedForbidden: 'Permission denied',
    saveFailedNotFound: 'Endpoint not found',
    saveFailedConflict: 'Conflict detected, please refresh and retry',
    saveFailedValidation: 'Validation failed',
    saveFailedRateLimit: 'Too many requests, please retry later',
    saveFailedUnavailable: 'Service temporarily unavailable',
    saveFailedUnknown: 'Save failed, please retry',
  },
}
