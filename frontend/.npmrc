# Pin exact versions on `pnpm add` so dependency versions don't float. We intentionally
# do NOT track pnpm-lock.yaml in git (see root .gitignore); production resolves from
# package.json at build time. Direct deps in package.json are already pinned exact.
save-exact=true
