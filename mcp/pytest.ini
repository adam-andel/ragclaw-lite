[pytest]
asyncio_mode = auto
testpaths = tests
# Run from the mcp root (/app in the mcp-repl container) so tests/ resolves to
# the server's own test suite next to repl_mcp_server.py / egress_proxy.py.
