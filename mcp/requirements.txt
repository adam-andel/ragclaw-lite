# Python REPL MCP Server — data-science runtime
# FLOATING "intent" source. The Dockerfile installs from the PINNED requirements.lock
# (generated by `pip freeze`) for reproducible builds. Edit ranges here, then regenerate
# the lock: pip install -r requirements.txt && pip freeze > requirements.lock
pandas>=2.0,<3.0
python-docx>=0.8,<2.0
python-pptx>=0.6,<1.0
matplotlib>=3.8,<4.0
fpdf2>=2.8,<3.0
pdfplumber>=0.11.0
reportlab>=4.0,<5.0
openpyxl>=3.1,<4.0
# Search skills (e.g. anysearch) ship a Python CLI (scripts/*_cli.py) that
# imports requests. Without it the Python runtime path fails and the agent
# falls through to node/bash (which OOM or lack jq) — burning tool rounds.
requests>=2.31,<3.0
# Transitive: numpy comes with pandas
